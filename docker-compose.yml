# Nexo — single-service local deployment.
#
# The bot is an OUTBOUND WebSocket client (wss://openws.work.weixin.qq.com);
# it binds no local port, so there is no `ports:` mapping.
# Secrets come from the gitignored .env at the repo root (env_file injects
# them into os.environ, so config.py's os.getenv works even though its
# load_dotenv path lookup no-ops inside the image).

services:
  nexo:
    build:
      context: .
      dockerfile: Dockerfile
    image: nexo:0.1.0
    container_name: nexo
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    # `nexo hello` exits 0 if the package imports cleanly — a lightweight
    # liveness probe for the install, NOT for the WS connection. A dead link
    # is handled by `restart: unless-stopped` above.
    healthcheck:
      test: ["CMD", "nexo", "hello"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
